# Retrieval Results

Selected retrieval benchmark outputs and visualizations copied from the project result runs.

Included:

- benchmark reports
- CSV and JSON retrieval outputs
- CPU run logs
- PDF summaries
- PNG visualizations for San Francisco and Kaluts query examples

Excluded:

- `.npz` embedding caches
- `.rar` archive bundles
- raw datasets and tile outputs
